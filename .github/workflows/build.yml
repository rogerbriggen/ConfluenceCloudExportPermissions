name: CI

on: [push]

jobs:
  build:
    name: build
    runs-on: ${{ matrix.operating-system }}
    strategy:
      matrix:
        operating-system: [windows-latest, ubuntu-latest] #macOS-latest
    #runs-on: [windows-latest]
       
    steps:

      - name: Checkout
        uses: actions/checkout@master


      - name: Setup dotnet env
        uses: actions/setup-dotnet@v1
        with:
            dotnet-version: '3.1.100' # SDK Version to use.


      - name: Build projects
        run: dotnet build --configuration Release src/ConfluenceCloudExportPermission.sln


      - name: Run unit-tests
        run: dotnet test --verbosity normal src/
      #- run: dotnet test --verbosity normal src/ConfluenceCloudExportPermission.sln

  publish:
    name: publish
    runs-on: [windows-latest]
    steps:

      - name: Publish for Win-x64 as SingleFile
        run: dotnet publish --configuration Release --runtime win-x64 /p:PublishSingleFile=true /p:PublishTrimmed=true ConfluenceCloudExportPermission/ConfluenceCloudExportPermission.csproj


      - name: Upload artifact
        uses: actions/upload-artifact@v1
        with:
            name: RemoteIncrediBuild_Release_x64
            path: ConfluenceCloudExportPermission\bin\Release\netcoreapp3.1\win-x64\publish\
